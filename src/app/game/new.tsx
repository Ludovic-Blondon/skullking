import { Placeholder } from '@/ui/screen';

export default function NewGameScreen() {
  return (
    <Placeholder title="Configuration de partie" phase="Phase P2">
      Sélection des joueurs, ordre autour de la table, et options repliées : édition de règles,
      cartes avancées, pouvoirs des pirates, variante de score.
    </Placeholder>
  );
}
