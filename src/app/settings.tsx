import { Placeholder } from '@/ui/screen';

export default function SettingsScreen() {
  return (
    <Placeholder title="Réglages" phase="Phase P6">
      Langue (FR/EN), thème, écran toujours allumé, édition de règles par défaut, export/import
      JSON, et la mention « non affilié à Grandpa Beck's Games ».
    </Placeholder>
  );
}
