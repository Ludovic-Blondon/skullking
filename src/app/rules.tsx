import { Placeholder } from '@/ui/screen';

export default function RulesScreen() {
  return (
    <Placeholder title="Aide-mémoire du barème" phase="Phase P6">
      Le barème complet de l'édition en cours, servi par le moteur de règles — jamais des valeurs
      codées en dur (PLAN.md, annexe A).
    </Placeholder>
  );
}
