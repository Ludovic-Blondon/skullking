import { Placeholder } from '@/ui/screen';

export default function HistoryScreen() {
  return (
    <Placeholder title="Historique" phase="Phase P3">
      Liste antichronologique des parties (date, joueurs, vainqueur, score), avec accès au détail
      complet de chaque feuille de score.
    </Placeholder>
  );
}
