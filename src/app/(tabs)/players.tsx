import { Placeholder } from '@/ui/screen';

export default function PlayersScreen() {
  return (
    <Placeholder title="Joueurs" phase="Phase P3">
      Roster persistant : création à la volée, tri par fréquence de jeu, archivage — et la fiche
      joueur avec ses statistiques.
    </Placeholder>
  );
}
